apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: bgd
  # Part of the route
  name: bgd
  # Second part of the route
  namespace: bgd
spec:
  host: ""
  port:
    targetPort: 8080
  to:
    kind: Service
    # Target
    name: bgd
    weight: 100
status: {}